<div id="<%= dom_id post %>">
  <p>
    <strong>タイトル:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>投稿日時:</strong>
    <%= post.created_at.strftime("%Y-%m-%d %H:%M") %>
  </p>

  <div class="blog-markdown-content">
    <%= markdown(post.body) %>
  </div>

</div>
